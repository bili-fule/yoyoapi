# Frontend API Base URL
# In separated deployment mode, point this to the backend server URL.
# In monolithic mode (default), keep commented out — uses '/api' (same origin).
# Inlined at build time for static export (NEXT_PUBLIC_ prefix).
# NEXT_PUBLIC_API_BASE=https://api.yoyoapi.example.com
